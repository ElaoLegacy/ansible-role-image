---

- name: packages
  apt: >
    name={{ item }}
    state=present
  with_items:
    - gifsicle
    - libjpeg-progs
    - libpng12-dev # NodeJS module "imagemin-optipng"
    - nasm # NodeJS module "imagemin-jpegtran"
  sudo: yes
